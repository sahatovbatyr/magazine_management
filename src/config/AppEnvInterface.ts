export interface AppEnvInterface {
  HOST: string,
  HOST_PORT: number,
  DB_HOST: string,
  DB_PORT: number,
  DB_USER: string,
  DB_NAME: string,
  DB_PASSWORD: string,
  EMAIL_SMTP_HOST: string,
  EMAIL_SMTP_PORT: number,
  EMAIL_USER: string,
  EMAIL_PASSWORD: string,

}